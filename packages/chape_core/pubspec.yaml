name: chape_core
description: Package partagé pour les applications ChapeChape Residence
version: 0.1.0
homepage: https://github.com/Loicqra12/chapechape_residence

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  yaml: ^3.1.1
  path: ^1.8.3
  path_provider: ^2.0.15
  crypto: ^3.0.3
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

# Configuration Flutter
flutter:
  # Le paramètre suivant est nécessaire pour la structure du package
  uses-material-design: false
